# 计算机网络

- 计算机网络（Computer networking）是一个将众多`分散的`、`自治的``计算机系统`，通过`通信设备与线路`连接起来，由功能完善的软件实现`资源共享和信息传递`的`系统`。

- `计算机网络`（简称网络）：由若干`结点（node）`和连接这些结点的`链路（link）`组成。

  - 结点可以是计算机、集线器、交换机、路由器等
    - 集线器(Hub):
      - 可以把多个结点连接起来，组成一个计算机网络
      - 普通民用领域已很少用集线器
      - “集线器”工作在物理层
    - 交换机(switch):
      - 可以把多个结点连接起来，组成一个计算机网络
      - 家庭、公司、学校通常用交换机组建内部网络
      - `作用:组件一个计算机网络`
      - 路由器(router):
        - 可以把两个或多个计算机网络互相连接起来，形成`规模更大的计算机网络`，也可称为`互连网`
        - 家用路由器=路由器+交换机+其他功能
        - `作用:连接两个或多个计算机网络`
  - 链路可以是有线链路、无线链路
  - `互连网`:多个计算机网络连接在一起
  - `互联网`（或因特网，Internet由各大`ISP`和国际机构组建的，覆盖`全球范围`的互连网(internet)
  - `互联网`必须使用`TCP/IP`协议通信，`互连网`可使用`任意协议`通信

  ---

# 组成划分

## 组成部分

1. 硬件:
   1. 主机:如：电脑、手机、物联网设备,(安装网卡(网络适配器))
      1. 网络适配器
         1. 将主机数据发到网络上
         2. 接收来自网络的数据
   2. 通信设备:如：集线器、交换机、路由器
   3. 通信链路:如：网线、光纤、同轴电缆
2. 软件:各种资源共享等软件
3. 协议:
   1. 规定计算机网络中的通信规则
   2. 由硬件、软件共同实现，如：网络适配器+软件实现网络通信协议

## 工作方式

1. `边缘`部分
   1. 工作方式:直接`为用户服务`(通信、资源共享)
   2. 主要由连接到互联网上的`主机及其软件`组成
2. `核心`部分
   1. 工作方式：为`边缘部分`提供`服务`(连通性、交交换服务）
   2. 由大量`网络`和连接这些网络的`路由器`组成

## 逻辑功能

1. 资源子网:
   1. 计算机网络中运行应用程序，向用户提供可共享的硬件、软件和信息`资源`的部分
   2. 主要由连接到互联网上的`主机`组成
2. 通信子网:
   1. 计算机网络中负责计算机间`信息传输`的部分。即把计算机和其他用户装置互连在一起的所有`通信设备和介质`的总称。
   2. 主要由`通信链路+通信设备+协议构成`
   3. 注：主机内部实现信息传输的网络适配器、底层协议属于通信子网的范畴

# 计算机网络的功能

1. 数据通信:实现计算机之间数据传输.是`最基本`,`最重要`的功能
2. 资源共享:硬件,软件,数据资源
3. 分布式处理:将某个复杂任务分配给网络中多台计算机处理
4. 提高可靠性:网络中各台计算机互为替代机
5. 负载均衡:网络中各台计算机共同分担繁重工作

---

# 分组交换

- 优点
  1. 通信前无需建立连接
  2. 数据以"分组"为单位被交换节点间"存储转发",通信线路可以灵活分配
  3. 在通信时间内,两个用户无需独占一整条物理线路.相比于电路交换,线路利用率高
  4. 交换节点支持"差错控制"
  5. 分组定长,方便存储转发管理
  6. 分组的存储转发时间开销小,缓存开销小
  7. 分组不易出错,重传代价低
- 新缺点
  1. 控制信息占比增加
  2. 依然存在存储转发时延
  3. 报文被拆分为多个分组，传输过程中可能出现失序,丢失等问题，增加处理的复杂度

## "虚电路交换"技术

1. 过程
   1. 建立连接(虚拟电路)
   2. 通信:`分组按序`,按已建立好的`既定线路`发送,通信双方`不独占`线路
   3. 释放连接

## 性能分析

![PixPin_2025-07-14_14-58-34](D:\refines\refines\PixPin_2025-07-14_14-58-34.png)

![PixPin_2025-07-14_15-00-43](D:\refines\refines\PixPin_2025-07-14_15-00-43.png)

# 计算机网络分类

1. 分布范围分类
   - 广域网(Wide Area Network),`WAN`
   - 城域网(Metropolitan Area Network),`MAN`
     - 通信技术:以太网技术
   - 局域网(Local Area Network)`LAN`
     - 通信技术:以太网技术
   - 个域网(Personal Area Network)`PAN`
     - 通常是通过无线技术将个人设备连接起来的网络，因此也常称为无线个域网（`WPAN`）
2. 传输技术分类
   - 广播室网络
     - 当一台计算机发送数据分组时，`广播范围`内`所有计算机`都会`收到`该分组，并通过检查分组的目的地址决定是否接收该分组
   - 点对点网络
     - 数据只会从`发送方`“`点对点`”发到`接收方`，`精准`送达
3. 拓扑结构分类
   - 总线型
     - 数据"广播式"传输
     - 存在总线争用问题
   - 环形
     - 数据"广播式"传输
     - 通过令牌解决总线争用问题,令牌顺环依次传递,拿到令牌者可使用总线
   - 星形
     - 由中央设备实现数据的"点对点"传输
     - 不存在"总线争用"问题
   - 网状
     - 数据通过各中间节点逐一存储转发
     - 点对点传输
4. 使用者分类
   - 公用网,专用网
5. 传输介质分类
   - 有线网络,无线网络

---

# 性能指标

1. 速率:(speed)
   - 信道:
     - 表示向某一方向传送信息的通道(信道 $\neq$ 通信线路)
     - 一条通信线路在逻辑上往往对应一条发送信道和一条接受信道
   - 定义:指连接到网络上的`节点`在`信道`上`传输数据`的`速率`。也称数据率或比特率、`数据传输速率`
   - 速率单位:bit/s,b/s,bps
   - 分类
     1. 波特率
        - 表示每秒传输几个码元
        - 单位:码元/秒,波特(Baud)
     2. 比特率
        - 表示每秒传输几个比特
        - 单位:bit/s,b/s,bps
2. 带宽(bandwidth)
   1. 某信道所能传送的最高数据率《计算机网络》，单位bps
   2. 某信道允许通过的信号频带范围《通信原理》，单位Hz
3. 吞吐量:（Throughput）:
   1. 指单位时间内通过某个网络（或信道、接口）的实际数据量。
   2. 吞吐量受带宽限制、受复杂的网络负载情况影响
4. 时延:(delay)
   1. 指数据（一个报文或分组，甚至比特）从网络（或链路）的一端传送到另一端所需的时间。有时也称为延迟或迟延。
   2. 总时延=发送时延+传播时延+处理时延+排队时延
   3. 发送时延: $=\frac{数据长度(bit)}{发送速率(bit/s)}$
      1. 又名传输时延节点将数据推向信道所花的时间
   4. 传播时延: $=\frac{信道长度(m)}{电磁波在信道中的传播速度(m/s)}$
      1. 电磁波在信道中传播一定的距离所花的时间
   5. 处理时延
      1. 被路由器处理所花的时间（如:分析首部、查找存储转发表)
   6. 排队时延
      1. 数据排队进入、排队发出路由器所花的时间
5. 时延带宽积
   1. $时延带宽积(bit) = 传播时延(s) \times 带宽(m/s)$
   2. 含义：一条链路中，已从发送端发出但尚未到达接收端的最大比特数
6. 往返时延
   1. RTT(Round-Trip Time）：表示从发送方发送完数据到发送方收到来自接收方的确认总共经历的时间
   2. <img src="D:\refines\refines\PixPin_2025-07-14_15-59-14.png" alt="PixPin_2025-07-14_15-59-14" style="zoom:50%;" />
7. 信道利用率
   1. 某个信道有百分之多少的时间是有数据通过的。
   2. $信道利用率=\frac{有数据通过的时间}{有数据通过的时间+没有数据通过的时间}$

---

# 分层结构

## 网络的体系结构

1. 网络的体系结构(Network Architecture)
   - 是计算机网络的各层及其协议的`集合`，就是这个计算机`网络及其构件`所应完成的`功能的精确定义`（不涉及实现）。
2. 实现
   - 实现(implementation)是遵循这种体系结构的前提下，用何种硬件或软件完成这些功能的问题。
3. 体系结构是抽象的,而实现则是具体的

## 各层之间的关系

1. 实体
   - 在计算机网络的分层结构中，第n层中的活动元素（软件+硬件）通常称为第n层实体。
   - 不同机器上的同一层称为`对等层`，，同一层的实体称为`对等实体`
2. 协议
   - 即网络协议(Network Protocol), 是控制对等实体之间进行通信的规则的集合，是`水平的`
3. 接口：
   - 即同一节点内`相邻两层的实体`交换信息的逻辑接口，又称为`服务访问点`(Service AccessPoint,SAP)
4. 服务：
   - 服务是指`下层为紧邻的上层`提供的功能调用，它是`垂直的`

## 分层结构:

- `协议数据单元`(PDU):`对等层次`之间传送的`数据单位`.第n层的PDU记为n-PDU
- `服务数据单元`(SDU):为完成`上一层实体`所要求的`功能`而传送的`数据`.第n层的SDU记为n-SDU
- `协议控制信息`(PCI):控制协议操作的信息,第n层的PCI记为n-PCI.

## 协议三要素

- 协议:即`网络协议`（Network Protocol），是控制对等实体之间进行通信的规则的集合，是`水平的`
- 三要素:
  1. 语法
     - 数据与控制信息的格式。例如，协议控制信息（首部）部分占几个字节、每个字节是什么含义；协议的数据部分最多由多少字节。
  2. 语义
     - 即需要发出何种控制信息、完成何种动作及做出何种应答。例如，协议中需要明确规定：发送方完数据后，接收方是否需要“应答”，以及应答的种类有哪些（如：传输成功、传输失败)
  3. 同步
     - 又称(时序),执行各种操作的条件、时序关系等，即事件实现顺序的详细说明。例如，发送方发完数据后，接收方需要立即应答。如果发送方在10秒内未收到“传输成功”应答，则发送方会再次发送数据。

## OSI参考模型

1. 物理层：实现相邻节点之间`比特`（0或1）的`传输`
   1. 传输单位:`比特(bit)`
   2. 需定义`电路接口参数`(如：形状、尺寸、引脚数等)
   3. 需定义`传输信号`的含义、`电器特征`(如:5V表示1,1V表示0;每比特电信号持续时间0.1ms)
2. 链路层:确保相邻节点之间的链路`逻辑上无差错`
   1. 传输单位:`帧(frame)`
   2. 差错控制：检错+纠错；或检错+丢弃+重传
   3. 流量控制：协调两个结点的速率
3. 网络层:把“分组"从源结点`转发`到目的结点
   1. 传输单位:`分组(Packet)`
   2. 路由选择：构造并`维护路由表`，决定分组到达目的节点的最佳路径
   3. 分组转发：将“分组”从合适的端口`转发`出去
   4. 拥塞控制;发现网络拥塞，并采取措施`缓解拥塞`
   5. 网际互联：实现`异构网络互联`
   6. 其他功能：差错控制、流量控制、连接建立与释放、可靠传输管理
4. 传输层:实现端到端通信（即实现进程到进程的通信，“端”指端口)
   1. 传输单位:`报文段(Segment)`
   2. `复用`和`分用`：发送端几个`高层实体`复用一条`低层的连接`，在接收端再进行分用。
   3. 其他功能：差错控制、流量控制、连接建立与释放、可靠传输管理
5. 会话层:管理进程间会话
   1. 会话管理
6. 表示层:解决不同主机上信息表示不一致的问题
   1. 数据格式转换
7. 应用层:实现特定的网络应用
   1. 传输单位:`报文(Message)`

## TCP/IP模型

#### 理念

- 如果某些应用需要数据格式转换会话管理功能，就交给应用层去实现
- 网络接口层为网络层传输“分组”。但TCP/IP 并未具体描述网络接口层的功能、协议（交给网络设备商自由发挥）

1. 网络接口层
   1. 实现相邻结点间的数据传输（为网络层传输“分组”）。但具体怎么传输不作规定
      使得具有更强的灵活性,适应性
2. 网络层
   1. 路由选择,分组转发,拥塞控制,网际互联
3. 传输层
   1. 复用和分用,差错控制,流量控制,连接建立与释放,可靠传输管理
   2. 协议
      1. TCP协议:有连接可靠
      2. UDP协议:无连接不可靠
4. 应用层
   1. 实现特定网络应用

# 物理层

- 任务:实现相邻节点之间比特(bit)的传输
- 实现物理链路：传输介质(0层) + 物理层(1层) 实现了相邻结点之间的"物理链路"

## 通信基础

1. 信源:信号的来源
2. 信宿:信号的归宿(即数据的"接收方")
3. 信道:信号的通道

### 信道的极限容量

1. 噪声:对信道产生干扰，影响信道的数据传输效率
2. 奈奎斯特定理(奈氏准则):
   1. 对于一个理想低通信道(即没有噪声,带宽有限的信道),其
      极限`波特率`=2W(单位:波特,即码元/秒)
      W:信道的频率带宽(单位:Hz)
   2. $极限比特率=2W~\log_2K~b/s$
      $K:码元一个周期内可能出现的K种信号$
   3. 定理说明
      1. 如果波特率太高，会导致“码间串扰”，即接收方无法识别码元
      2. 带宽越大，信道传输码元的能力越强
      3. 奈奎斯特定理并未对一个码元最多可以携带多少比特做出解释
3. 香农定理:
   1. 对于一个有噪声,带宽有限的信道,其
      极限`比特率`=$W\log_2(1+\frac{S}{N})$(单位:b/s)
      W:带宽      S/N:信噪比
   2. 信噪比=$\frac{S}{N}=\frac{信号的功率}{噪声的功率}$(无单位)
   3. 以"分贝(dB)"为单位表示信噪比
      $信噪比=10\log_{10}~\frac{S}{N}$(单位:分贝dB)
   4. 考试时使用无单位记法
   5. 定理说明
      1. 提升信道带宽、加强信号功率、降低噪声功率，都可以提高信道的极限比特率
      2. 结合奈奎斯特定理，可知，在带宽、信噪比确定的信道上，一个码元可以携带的比特数是有上限的

---

1. 信号:数据的载体
   1. 数字信号:值是离散的,例如:$信号值y\in \{-1,1\}$
   2. 模拟信号:值是连续的,例如$信号值y\in [-1,1]$
2. 码元:每一个信号就是一个码元
   1. 信号周期也可以称为"码元宽度"
   2. 如果一个码元（即一个信号）可能有4种状态，那么可以称其为 4进制码元（一个码元携带2bit数据）
   3. 一个码元可以携带`多少比特的数据`,取决于一个周期内可能出现的`K种信号,`即
      $1码元=\log_2K~bit$
   4. $比特率=M\times n ~~b/s$
      $M:波特率$           $n:一个码元携带n比特的信息量$

## 编码和调制

- 编码:将二进制数据转换为`数字信号`
- 解码:将`数字信号`转化为二进制数据
- 调制:将二进制数据转换为`模拟信号`
- 解调:将`模拟信号`转换为二进制数据

### 编码方法

1. 不归零编码(NRZ)
   1. 低0高1,中不变
   2. 特点
      1. 自同步能力:无
         1. 自同步能力:信源和信宿可以根据信号完成“节奏同步”，无需时钟信号
      2. 是否浪费带宽:不浪费
      3. 抗干扰能力:弱
2. 归零编码(RZ)
   1. 低0高1,中归零(起到时钟作用)
   2. 特点
      1. 自同步能力:有
      2. 是否浪费带宽:浪费
      3. 抗干扰能力:弱
3. 反向非归零编码(NRZI)`Non-Return-to_Zero Inverted`
   1. 跳0不跳1看起点,中不变
   2. 上跳0,下跳1
   3. 特点
      1. 自同步能力:若增加冗余位,可支持自同步
      2. 是否浪费带宽:浪费一点
      3. 抗干扰能力:弱
4. 曼彻斯特编码
   1. 跳0反跳1看中间,中必变
   2. 特点
      1. 自同步能力:有
      2. 是否浪费带宽:浪费
      3. 抗干扰能力:强
5. 差分曼彻斯特
   1. 跳0不跳1看起点,中必变
   2. 特点
      1. 自同步能力:有
      2. 是否浪费带宽:浪费
      3. 抗干扰能力:强

### 调制方法

基带信号:来自信源的数字信号,需调制后才能在某些信道上传输

1. 调幅AM(又称幅移键控ASK)
   1. $y=\begin{cases}0\vdot\sin 2x,~~~表示二进制0  \\ 1\vdot\sin 2x,~~~表示二进制1 \end{cases}$
2. 调频FM(又称频移键控FSK)
   1. $y=\begin{cases}\sin 1\vdot x,~~~表示二进制0 \\ \sin2\vdot x,~~~表示二进制1\end{cases}$
3. 调相PM(相移键控PSK)
   1. $\begin{cases}\sin(x+0),~~~表示二进制0 \\ \sin(x+\pi),~~~表示二进制1 \end{cases}$
4. 正交幅度调制(QAM)
   1. 将AM,PM结合起来,形成叠加信号
   2. 若设计m种幅值,n种相位,则将AM,PM信号两两"复合",可调制出mn种信号,则QAM的1码元=$\log_2mn~bit$

## 传输介质

- 常用传输介质
  - 导向型:信号朝固定方向传播
    - 双绞线(Twisted Pair)
      1. 两根导线相互绞合而成
         - 有屏蔽层=屏蔽双绞线(STP)Shielded
         - 没有屏蔽层=非屏蔽双绞线(UTP）unShielded
      2. 抗干扰能力:较好。绞合、屏蔽层层可以提升抗电磁干扰能力
      3. 改善:提高绞合度、增加屏蔽层
      4. 代表应用一近些年的局域网早期电话线
    - 同轴电缆
      - 内导体(传输信号) + 外导体屏蔽层(抗电磁干扰)
      - 抗干扰能力一好。月屏蔽层带来良好的抗干扰性
      - 代表应用一早期局域网、早期有线电视
    - 光纤
      1. 构成
         - 纤芯(高折射率) + 包层(低折射率)
         - 利用光的全反射特性，在纤芯内传输光脉冲信号
      2. 分类
         1. 单模光纤一只有一条光线在一根光纤中传输，适合长距离传输，信号传输损耗小
            - 特点：纤芯更粗，可同时传输多条光线，信号传输损耗更高。适合较近距离传输
         2. 多模光纤千一多条光线在一根光纤中传输，适合近距离传输，远距离传输光信号容易失真
            - 纤芯更细，直径小于一波长只能传输一条光线，信号传输损耗低。适合远距离传输
      3. 抗干扰能力一非常好。光信号对电磁干扰不敏感
      4. 其他优点—一信号传输损耗小，长距离传输时中继器少；很细很省布线空间
  - 非导向型:
    - 信号朝四面八方传播
    - 无线传输介质
      1. 无线电波
         - 特点:  穿透能力强, 传输距离长, 信号指向性弱
         - 如:手机信号,WiFi
      2. 微波通信
         - 特点:频率带宽高、信号指向性强、保密性差(容易被窃听)
         - ·如：卫星通信(卫星作为信号中继器，传播时延较大)
      3. 其他：红外线通信、激光通信等一信号指向性强
- 以太网对有线传输介质的命名规则
  - 速度+Base+介质信息
  - 10Base5:    10MbpS，同轴电缆，最远传输距离500m

## 物理层接口的特性

- 机械特性: 指明接口所用接线器的形状和尺寸、引脚数目和排列、固定和锁定装置 等
- 电气特性: 指明在接口电缆的各条线上出现的电压的范围传输速率、距离限制等
- 功能特性: 指明某条线上出现的某一电平的电压的意义
- 过程特性(规程特性): 指明对于不同功能的各种可能事件的出现顺序

## 物理层设备

- 中继器(repeater)
  - Eg：物理层“电气特性”规定一0.5 1.5V是低电平，4.5~5.5V是高电平，不符合此标准的信号视为无效
    中继器接收到信号后，会会将低电平整形为1V，将高电平整形为5V，然后再输出
  - 中继器只有`两个端口`。通过一个端口`接受信号`，将失真信号整形再生，并`转发至另一个端口`(信号再生会产生一些时延)
  - 仅支持`半双工通信`(两端连接的节点不可同时发送数据,会导致"冲突")
  - 中继器两个端口对应`两个"网段"`
- 集线器(hub)
  - 本质上是`多端口中继器`.集线器将其中一个端口接受到的信号整形再生后,转发到所有其他端口
  - 各端口连接的节点`不可同时发送`数据,会导致`"冲突"`
  - 集线器的N个端口对应N个`"网段"`,各网段属于同一个`"冲突域"`
  - 集线器 连接的网络，`物理`上是`星形拓扑`，`逻辑`上是`总线型拓扑`
  - 集线器连接的各网段`“共享带宽”`
  - 集线器可以连接`不同的传输介质`, 因此两个网段的物理层接口特性可以不同 (这就意味着集线器连接的网段“物理层协议”可以不同）
  - 集线器如果连接了速率不同的网段，会导致所有网段“速率向下兼容”
- 冲突域(碰撞域)
  - 如果两台主机同时发送数据会`导致“冲突”`，则这两台主机处于`同一个“冲突域"`
  - 处于同一冲突域的主机在发送数据前需要进行`信道争用`
  - 集线器`不能"隔离"`冲突域

- 集线器和中继器: `不能“无限串联"`  如：10Base5 的 5-4-3 原则
- `5-4-3原则`：使用集线器（或中继器）连接10Base5网段时，最多只能串联5个网段，使用4台集线器（或中继器），只有3个网段可以挂接计算机。

# 数据链路层

- 逻辑链路: 数据链路层需要基于"物理链路", 实现相邻节点之间逻辑上无差错的"数据链路(逻辑链路)"

## 功能

1. 封装成帧(组帧)
   1. 确定帧的界限(帧定界)
   2. 透明传输
      - 接收方链路层要能从`收到的帧内`为`恢复原始SDU`让网络层“感受不到"将分组封装成帧的过程
   3. 差错控制
      1. 发现并解决一个帧内部的"位错"
         1. 解决方案一：接收方发现比特错后`丢弃帧`，发送方`重传帧`(仅需采用检镜编码)
         2. 解决方案二：由`接收方发现并纠正比特错误`（需采用纠错编码）
   4. 可靠传输
      1. 发现并解决帧错误
         1. 帧丢失
         2. 帧重复
         3. 帧失序
   5. 流量控制
      1.  控制发送方发送帧的速率,让接收方"来的及"接收
   6. 介质访问控制
      1. "广播信道"需要实现此功能.广播信道在逻辑上是总线型拓扑, 多个节点需争抢传输介质的使用权
      2. "点对点信道"通常不需要. 点对点信道通常意味着两个节点之间有专属的传输介质,不用抢
   

## 组帧

### 主要问题

1. 帧定界: 如何让接收方能够确定帧的界限
2. 透明传输: 接收方要能去除"帧定界"的附加信息, 把帧"恢复原貌"

### 组帧方法

1. 字符计数法
   1. 原理: 在每一个帧的开头, 用一个定长计数字段表示帧长
   2. 帧长=计数字段长度+帧的数据部分长度
   3. 最大缺点：任何一个计数字段出错都会导致后续所有帧无法定界
2. 字节填充法
   1. 控制字符(SOH)
   2. 控制字符(EOT)
   3. 转义字符(ESC)
   4. 如果帧的数据部分包含“特殊字符”，则发送方需要在这些‘特殊字符”前填充“转义字符ESC"（接收方要做逆处理)
3. 零比特填充法
   1. 特殊比特串`01111110`表示帧开始/结束
   2. 发送方需要对帧的数据部分进行处理，每当遇到连续5个1, 就填充一个0
   3. 接受方需要对帧的数据部分进行逆处理，每当遇到连续5个1，就删掉后面的0
   4. HDLC,协议 PPP协议使用此方法
4. 违规编码法
   1. 基于曼彻斯特编码
   2. 如果周期中间不跳变，则违规
   3. 物理层在帧的前面和后面分别插入一个时钟周期的违规信号

## 差错控制

发现并解决一个帧内部的"位错"

1. 检错编码
   1. 奇偶校验法
      1. 原理: 
         1. 奇校验码：整个校验码（有效信息位和校验位）中“1"的个数为奇数。
         2. 偶校验码：整个校验码（有效信息位和校验位）中“1"的个数为偶数。
            1. 偶校验的硬件实现：各信息进行异或（模2加）运算，得到的结果即为偶校验位
            2. 无法检测出偶数位错误
         3. 奇偶校验码仅能检测出奇数位错误无纠错能力
      2. 概念
         1. 信息位(有效数据): 指帧的"数据部分'
         2. 校验位（冗余位）：即为了给帧的"数据部分"检错/纠错而附加的一些冗余比特
   2. CRC校验码(循环冗余校验法)
      - 原理
        1. 数据发送、接受方约定一个“除数"
        2. <u>K个信息位+R个校验位</u>作为"被除数”，添加校验位后需保证除法的余数为0
        3. 收到数据后, 进行`除法检查`余数`是否为0`
        4. 若余数非0说明出错，则进行重传或纠错
        5. 余数为0，说明无错误
           余数非0，说明出错
      - ![image-20250721101310284](C:\Users\30703\AppData\Roaming\Typora\typora-user-images\image-20250721101310284.png)
      - ![image-20250721101529927](C:\Users\30703\AppData\Roaming\Typora\typora-user-images\image-20250721101529927.png)
      - 只有检错的能力, 没有纠错的能力
      - K个信息位，R个校验位，若生成多项式选择得当，，且$2^R$≥K+R+1则CRC码可纠正1位错
      - 理论上可以证明循环冗余校验码的检错能力有以下特点：
        1）可检测出所有奇数个错误；
        2）可检测出所有双比特的错误;
        3）可检测出所有小于等于校验位长度的连续错误;
2. 纠错编码
   1. 海明校验码(汉明码)
      1. 原理
         - 偶校验码：整个校验码（有效信息位和校验位）中“1"的个数为偶数。
         - 海明码设计思路: 将信息位分组进行行偶校验 ->多个校验位 -> 多个校验位标注出错位置
      2. 求解步骤
         1. 确定校验位个数（k个校验位，n个信息位)    $2^k$ ≥ n+k+1
            - <img src="C:\Users\30703\AppData\Roaming\Typora\typora-user-images\image-20250721153934214.png" alt="image-20250721153934214" style="zoom:50%;" />
         2. 确定校验位分布
            - P1, P2, P3..... 分别在1, 2, 4, 8, 16.....
            - 空出来的其他位置依次填入信息位
            - <img src="C:\Users\30703\AppData\Roaming\Typora\typora-user-images\image-20250721153914823.png" alt="image-20250721153914823" style="zoom:50%;" />
         3. 求校验位
            - 将信息位的位置序号用k位二进制数表示出来
            - 校验位Pi 与 位置序号第i位为1的信息位归为同一组, 进行偶校验
            - <img src="C:\Users\30703\AppData\Roaming\Typora\typora-user-images\image-20250721154021749.png" alt="image-20250721154021749" style="zoom:50%;" />
         4. 纠错
            - 对P1，P2,P3...所属各分组进行异或（相当于分组偶校验）求得S1，S2，S3..
            - S3 S2 S1=000说明无错误
            - S3 S2 S1$\neq$000，则其值反映出错位置
            - <img src="C:\Users\30703\AppData\Roaming\Typora\typora-user-images\image-20250721154058209.png" alt="image-20250721154058209" style="zoom:50%;" />
      3. 补充
         - 海明码有1位纠错，2位检错能力
         - 为了区分1位错和2位错，还需添加“全校验位”对整体进行偶校验
         - 有的题目位置编号可能是从小到大的,处理方法雷同

## 流量机制和可靠传输机制

1. 流量机制, 可靠机制, 滑动窗口机制

   - 三种协议, 本质上都是`滑动窗口机制`

   - #### 停止-等待协议(S-W): 

     - 滑动窗口机制: 发送窗口$W_T$=1， 接收窗口Wr=1
     - 确定机制: 若接收方收到`i`号帧，且没有检测出"差错"，需要给发送方返回`确认帧ACK_i`(acknowledge)
     - 重传机制: 若发送方超时未收到ACK_i,则重传i号帧
     - 帧编号
       - 仅需1bit给帧"编号"
       - 要求: $W_T + W_R \le 2^n$
     - 信道利用率:
       - 理想情况下: $信道利用率=\frac{传输数据帧的时间}{传输时延+传播时延+传输确认帧的时间}=\frac{T_D}{T_D+RTT+T_A}$

   - #### 后退N帧协议(GBN Go-Back-N): 

     - 滑动窗口机制: 发送窗口$W_T$>1， 接收窗口Wr=1
     - 特殊规则:
       - 关于确认帧：接收方可以“累积确认"。即连续收到多个数据帧时，可可以仅返回最后一个帧的ACK(ACK_i表示接收方已收到i号帧及其之前的所有帧)
       - 关于超时重传：若发送方超时未收到ACK_i，则重传i号帧，及其之后的所有帧
       - 收到一个"非法帧"时，接收方会将此帧丢弃，并返回目前已接收的最后一个正确帧的 ACK_i，以提醒发送方"后退"回i+1号帧重新发送
       - 注：“非法帧"包括落在接收窗口之外的帧、检测出差错的帧
     - 原本已经发送了1号帧，现在却“后退”回0号帧重新传送这样即可实现`“流量控制"`
     - 缺点：如果接收方接收帧的速度很慢，或在信道误码率很高的情况下，可能会导致发送方的发送进度经常需要“后退”，传输效率低下
     - 信道利用率:
       - $信道利用率=\frac{N T_D}{T_D+RTT+T_A}$
       - 如果 $NT_D$>$T_D + RTT + T_A$   则最大信道利用率=1
       - 有时题目会忽略$T_A$即传输确认帧的时间
   
   - #### 选择重传协议:(selective Repeat) 
   
     - 滑动窗口机制: 发送窗口$W_T$>1， 接收窗口Wr>1
     - 特殊规则
       - 选择重传协议规定：接收方可以连续接收多个帧，但每个帧都需要返回ACK
       - 否认帧：:NAK_i
         - 若接收方收到i号帧但检测出i号帧有"差错"，需要丢弃该帧并给发送方返回否认帧NAK_i
         - 请求重传: 若发送方收到NAK_i则重传i号帧
         - 要求：$W_R$≤ $W_T$，即接受窗口不能大于发送窗口
       - 在实际应用中，通常取$W_R$==$W_T$
       - 超时重传机制：每个帧被发出时设置计时器如果超时未收到对应的ACK，就重传这个帧
       - 请求重传机制：如果接收方收到一个有差错的帧，就将此帧丢弃，并返回对应的否认帧NAK_i，主动请求发送方重传i号帧
   
   - 发送窗口内的帧允许被发送
   
   - 接收窗口内的帧允许被接受
   
   - n相同时，GBN协议的$W_T$更大n至少为4，才能确保$W_T$≥7.2
   
   - 术语
   
     - 滑动窗口协议: 指GBN协议或SR协议
     - ARQ; 指S-W或GBN或SR协议
     - 连续ARQ协议: 指GBN或SR协议

## 介质访问控制

- 介质访问控制(`Medium Access Control, MAC`): 多个节点共享一个"总线型"广播信道时, 可能发生"信号冲突"

#### 分类

##### 	信道划分

- 时分复用(TDM)
   - 时分复用（`Time DivisionMultiplexing，TDM`）－－将时间分为等长的`“TDM帧”`，每个“TDM帧”又分为等长的m个`“时隙”`，将m个时隙分配给`m对用户`（节点）使用
   - 缺点:
      - 每个节点最多只能分配到信道总带宽的$\frac{1}{m}$
      - 如果某节点暂不发送数据，会导致被分配的“时隙”闲置，信道利用率低
         - 解决方案: 可统计每个节点对信道的使用需求动态按需分配时隙
            (`即统计时分复用`)
- 统计时分复用(STDM)
   - 统计时分复用( `Statistic Time Division Multiplexing,STDM`)一一又称异步时分复用在TDM的基础上，动态按序分配时隙
   - 优点:
      - 如果需要时，一个节点可以在一段时间内获得`所有的`信道`带宽资源`
      - 如果某节点暂不发送数据，可以不分配“时隙”，`信道利用率更高`
- 频分复用技术(FDM)
   - 如果两个信号频率区别很大，就很容易“分辨”出来
   - 频分复用（`FrequencyDivision Multiplexing，FDM`）是将信道的总频带划分为多个子频带，每个子频带作为一个子信道，每对用户使用一个子信道进行通信
   - 隔离频带: 防止子通道间相互干扰
   - 复用器: 将各节点发出的信号“复合”后传输到共享信道上
   - 分用器: 将各个子频带信号“分离”出来
   - 优点: 各节点可同时发送信号；充分利用了信道带宽(Hz)
   - 缺点: FDM技术只能用于模拟信号的传输
- 波分复用:
   - 波分复用(`Wavelength Division Multiplexing,WDM`）—－即即光的频分复用
   - 复用器: 将各节点发出的不同波长的光信号“复合”后传输到到光纤上
   - 分用器: 将不同波长的光信号“分离出来
   - ==Tips==：光信号的频率范围（带宽）非常大，因此很适合采用波分复用技术，将一根光纤在逻辑上拆分为多个子信道
- 码分复用: 
   - 实现:
      1. 给各节点分配专属"码片序列"
         - 码片序列"包含m个码片(信号值)可看作"m维向量"(m维向量的分量通常取1或-1)
         - 要求：各节点的"m维向量"必须`相互正交`
         - Tips: 相互通信的各节点知道彼此的“码片序列"
      2. 发送方发送数据
         - 节点发出m个信号值与“码片序列"相同，表示比特1
         - 节点发出m个信号值与“码片序列"相反，表示比特0
      3. 信号在传输过程中"叠加"
         - 当多个发送方同时发送数据时，信号值会叠加（注：本质是多个m维向量的加法）
      4. 接收方接受数据
         - 接收方收到的是“叠加"信号，需要从中“分离”出各发送方的数据
         - 叠加信号与发送方的码片序列作“规格化内积”
            - 结果为1，表示比特1
            - 结果为-1，表示比特0

#### 随机访问

1. ALOHA协议
   - 纯ALOHA协议
      - 如果准备好数据帧，就立刻发送
   - 时隙ALOHA协议
      - 时隙大小固定=传输一个最长帧所需时间
      - 只有在每个时隙开始时才能发送帧
      - 时隙ALOHA`避免`了用户发送数据的`随意性`，降低了冲突概率，提高了信道的利用率
      - 时隙 ALOHA发生冲突时，发生冲突的各节点随机推迟若干个时隙
2. CSMA协议
   -  `Carrier Sense Multiple Access`协议，即载波监听多路访问协议
   - CSMA协议在ALOHA协议基础上提出改进：
      - 在发送数据之前，先监听信道是否空闲，只有信道空闲时，才会尝试发送
   - 1-坚持-CSMA协议
      - 坚持监听信道是否空闲
      - 优点：信道利用率高，信道一旦空闲，就可以被下一个节点使用。
      - 缺点：当多个节点都已准备好数据时，一旦信道空闲，会有多个节点同时发送数据，冲突概率大
   - 非坚持-CSMA协议
      - 若信道不空闲, 放弃监听信道, 随机推迟一段时间
      - 优点：当多个节点都已准备好数据时，如果信道不空闲则各节点会随机推迟一段时间再尝试监听，从而使各节点错开”发送数据，降低冲突概率。
      - 缺点：信道刚恢复空闲时，可能不会被立即利用，导致信道利用率降低
   - p-坚持CSMA协议
      - 坚持监听信道.  若信道空闲, 有概率p立刻把"数据帧"发送到信道上, 有1-p概率推迟一段时间再尝试发送
      - 优点：属于 1-坚持CSMA、非坚持CSMA 的折中方案，降低冲突概率的同时，提升信道利用率
3. CSMA/CD(`Collision Dectection`)协议: 冲突检测
   1. 协议要点：
      1. 先听后发, 边听边发, 冲突停发, 随机重发
      2. 随机重发
         1. 截断二进制指数退避算法: `随机等待一段时间 = r倍争用期`,  其中r是随机数
         2. 如果 k ≤ 10, 在[0, (2^k - 1)]区间随机取一个整数r
         3. 如果 k > 10, 在[0, (2^10 - 1)]区间随机取一个整数r
         4. 第10次冲突, 是"随机重发"的分水岭
         5. 第16次冲突, 直接躺平, 放弃传帧, 报告上级(网络层)
      3. 争用期 = 2 $\times$ 最大单向传播时延(考虑距离最远的两个节点)
         1. 若争用期内未发生冲突，、足就不可能再冲突
         2. CSMA/CD没有ACK机制，，若发送过程中未检测到冲突，就认为帧发送成功
      4. 最短帧长 = 2 × 最大单向传播时延 × 信道带宽. 若收到的帧小于最短帧长，视为无效帧
         1. 举例: 如果A节点发送的数据帧长度小于600bit，可能会导致A节点“误以为”发送过程中没有发生冲突，但实际上已经发生了冲突
      5. 最长帧长：规定最长帧长可防止某些节点一直占用信道
      6. 以太网规定: 最短帧长=64B；最长帧长=1518B
      7. 适用于有线网络（如:以太网技术)
4. CSMA/CA(`Collison Avoidance`)协议: 冲突避免
   1. 发送过程中不用检测冲突，发送前想办法尽量避免冲突（但无法完全避免）
   2. 适用于无线网络
      （如：IEEE 802.11标准的无线局域网技术，即WiFi）
   3. WiFi为什么不使用CSMA/CD？
      1. 硬件上很难实现见“边听边发，冲突检测”十一因为接收信号的强度往往远小于发送信号的强度，且在无线介质上信号强度的动态变化范围很大
      2. 存在`“隐蔽站”`问题: 在无线通信中，并非所有站点都能够听见对方。发送结点处没有冲突并不意味着在接收结点处就没有冲突。
   4. 信道预约机制
      1. 先预约, 再发送
      2. RTS控制帧(`Request To Send`)
         - 请求发送
         - 包括源地址, 目的地址和这次通信所需的持续时间
      3. CTS控制帧(`Clear To Send`)
         - 允许发送
         - 包括源地址, 目的地址和这次通信所需的持续时间
      4. 实现
         1. 发送方广播RTS控制帧
         2. AP广播CTS控制帧
         3. 其他无关节点收到CTS后自觉"禁言"一段时间, 发送方收到CTS后, 就可以发送数据帧
         4. AP收到数据帧后, 进行CRC校验, 若无差错就返回ACK帧
      5. 根据数据帧长度决定是否使用信道预约机制(减小错误代价)
   5. AP(Access Point) 接入点: 也就是平时连接的无线WiFi热点
   6. 协议要点:
      1. 先听后发, 忙则退避
         若信道空闲，间隔DIFS后，再发送帧页（一口气发完，发送过程中不用检测冲突
         若信道不空闲，则进行“随机退避”
      2. 随机退避
         1. 用二进制指数退避算法确定一段随机退避时间(倒计时)
         2. 发送方会保持监听信道，只有信道空闲时才“扣除倒计时”, 倒计时结束后立即发送帧
      3. 接收方
         1. 停止等待协议
         2. 每收到一个正确数据帧都返回ACK帧
         3. 若发送方超时未收到ACK帧, 则进行"随机退避"
   7. 帧间间隔IFS(`Inter Frame Gap`)
      - `DIFS`(分布式协调IFS)`distributed inter frame gap`
         - 最长的IFS
         - 每次"帧事务"开始之前需要等待的时间
         - 不属于通信会话内部的时间
         - 是留给未来竞争者的等待时间
      - `SIFS`(短IPS)
         - 最短的IFS
         - 收到一个帧后需要预留一段处理时间(如差错控制等)
      - `PI`FS
         - 中等长度的IFS(了解即可)
   8. 网络分配向量NAV
      - 其他节点延迟访问的时间
      - NAV = SIPS时间 + 数据帧发送时间 + SIPS时间 + ACK帧传播时延

#### 轮询访问

- 令牌传递协议
  - 令牌帧
    - 指明当前获得令牌的节点编号
    - 只有获得令牌(Token)的节点才能往信道上发送数据帧
    - 如果获得令牌的节点没有数据要发送, 就将令牌传递给下一个节点
  - 数据帧
    - 发送数据时由令牌帧转换成数据帧
    - 需指明数据帧的源地址, 目的地址, 是否已被接受, 获得令牌的节点编号
    - 数据帧从源节点发出, 传递一圈后回到源节点
    - 数据帧"传递一圈"的过程中, 会被目的节点复制一份数据, 并将数据帧标记位"已接收"
    - 数据帧回到源节点后, 如果发现异常状况, 就尝试重发, 若无异常, 就将令牌传递给下一个节点
  - 无论是令牌帧还是数据帧，都只能沿单向传递
  - 获得令牌的节点，每次只能发一帧，发完就释放令牌
  - 需要用专门的网络设备备（MAU）实现集中控制
  - 令牌传递协议很适用于负载高的网络各（不会发生冲突，效率高)

## 局域网

局域网（LAN）

- 特点
  - 覆盖较小的地理范围
  - 较低的时延和误码率
  - 局域网内的各节点之间
  - 帧”为单位进行传输
  - 支持单播、广播、多播
- 局域网三要素
  1. 拓扑结构
  2. 传输介质
  3. 介质访问控制方式
- 分类
  - 有线局域网
    1. 令牌传递(token ring)
       - 1984~2000年
       - 环形
       - 同轴电缆或双绞线
       - 令牌传递协议
    2. 以太网/802.3
       1. 1980年开始商用
          - 同轴电缆以太网
          - 总线形
          - 同轴电缆 10Base5  1982年
          - CSMA/CD协议
          - 物理层使用曼彻斯特编码
       2. 20世纪80年代后期
          - 双绞线以太网
          - 10BaseT   1990年
          - 用集线器连接
            - 物理上星型, 逻辑上总线型
            - CSMA/CD协议
            - 半双工通信
          - 用交换机连接
            - 星形
            - 双绞线
            - CSMA/CD或NULL协议
            - 半双工模式下, 采用CSMA/CD协议来处理信道争用
            - 全双工模式下, 不用争抢信道
       3. 20世纪90年代初
          - 光纤以太网
          - 10BaseF   1993年
          - 点对点: 光纤通常不会直接连接终端节点
          - 光纤
          - 用两台光纤实现全双工通信(无信道争用问题)
  - 无线局域网(WLAN)
    - WiFi/802.11
    - IEEE 802.11定义为星形结构(1个AP+N台设备)
    - 无线
    - CSMA/CA协议
- 网络适配器
  1. 网络接口卡, 网卡
  2. 负责把帧发送到局域网
  3. 负责从局域网接收帧
     1. 如果收到正确帧, 就用"中断"通知CPU; 如果收到异常帧, 就直接丢弃
  4. 根据接入的局域网类型, 按照标准实现数据链路层+物理层功能
  5. 完成数据的串/并行转换
  6. 支持帧缓冲

## IEEE 802.3

- 双绞线速率<2.5Gbps时, 可支持半双工或全双工
  速率≥2.5Gbps时, 只支持全双工
- 同轴电缆只支持半双工
- 光纤只支持全双工
- 交换机连接的终端节点都可以全双工
- 用集线器连接的节点，仅支持半双工模式
- 数据链路层的MAC层的协议
  - DEC、Intel、Xerox 推出的`DIX Ethernet V2`标准
  - `MAC帧`
    - 记忆口诀: 662N4,收发协数验
    - ![image-20250723104131136](C:\Users\30703\AppData\Roaming\Typora\typora-user-images\image-20250723104131136.png)
    - 目的地址全1表示"广播帧"
    - 注意：路由器R收到广播帧后，不会再转发至其他网络, 只有同一个局域网内的各节点才属于同一个“广播域
- 以太网帧
  - `以太网帧 ⊂ MAC帧`
  - 特指在 **以太网（Ethernet）技术** 下的MAC帧。
  - 它是MAC帧的一种实现。
  - 结构一般同MAC帧


## VLAN

- IEEE 802.1Q工作组负责
- 虚拟局域网
- 可将一个大型局域网分割成若干个较小的VLAN,每个VLAN是一个广播域
- 每个VLAN对应一个VID
- 需要使用支持VLAN功能的以太网交换机来实现
- VLAN划分方式
  1. 基于接口
     - 交换机根据VID所对应的接口号划分
  2. 基于MAC地址
     - 交换机根据VID配置对应的MAC地址
  3. 基于IP地址
     - 交换机根据VID配置对应的IP地址
     - 可以让VLAN范围跨越路由器, 让多个局域网的主机组成一个VLAN(需要网络层支持)
- 802.1Q帧
  - 由标准以太网帧转化而来
  - 主机与交换机之间，传输标准以太网帧
  - 交换机与交换机之间，传输802.1Q帧
  - 802.1Q帧包含VID信息

## 无线局域网

- 802.11工作组负责
- 星形拓扑结构
- 基本服务集 BSS：1个基站(AP)+多个移动站
  - 服务集标识符SSID:也就是无线局域网的名字，不超过32字节
  - 基本服务区BSA：指一个基本服务集能够覆盖的地理范围
- 扩展服务集ESS: 将多个AP连接到同一个分配系统，组成一个更大服务集
- 漫游：一个移动站从一个基本服务集切换到另一个基本服务集，仍然可以保持通信
- 门户（Portal）：可将802.11无线局域网接入802.3有线以太网
- 802.11帧
  - 数据帧
  - 控制帧
  - 管理帧
    - 探测请求帧
    - 探测响应帧
  - ![image-20250723111155042](C:\Users\30703\AppData\Roaming\Typora\typora-user-images\image-20250723111155042.png)
  - 在802.11无线局域网内，两个移动站之间不能直接通信, 必须通过基站(AP)转发

## 以太网交换机

- 自学习功能
  - 交换表
    - 初始为空
    - 记录端口号和MAX地址的对应关系
  - 每收到一个帧，就将发送方”【MAC地址，端口号】更新到交换表
  - 如果不知道“接收方“在哪里, 就把帧广播到除入口外的其他端口
  - 如果知道“接收方”在哪里, 就把帧精准转发至某个端口
  - 交换表中每个表项都有“有效时间"，过期表项自动作废(以防某些节点拔线跑路)
- 特点
  - 多端口网桥
  - 交换机工作在数据链路层, 可以根据目的MAC地址转发帧
- 两种交换方式
  1. 直通交换
     - 交换机的高速缓存只接收并处理目的地址
     - 优点: 转发时延低
     - 缺点: 不适用于需要速率匹配, 协议转换或差错检测的线路
  2. 存储转发交换
     - 优点: 适用于需要速率匹配, 协议转换或差错检测的线路
     - 缺点: 转发时延高

# 网络层

- 为传输层提供服务, 将传输层的数据封装成“IP数据报"
- 网络中的路由器根据IP数据报首部中的源IP地址, 目的IP地址进行"分组转发"
- 网络层实现了"主机到主机"的传输

## 功能

### 异构网络互联

- 异构: 每个网络的拓扑结构不同物理层&链路层的实现不同、主机类型也各不相同
- 重要设备: 路由器(Router)
- 在TCP/IP文献中, 路由器也称为网关(Gateway)

### 路由与转发

#### 路由

- 各个路由器之间相互配合规划IP数据报(分组)的最佳转发路径
- 各个路由器需要运行路由协议, 最终生成各自的“路由表”

#### 转发

- 一台路由器，根据自己的“转发表”，将收到的IP数据报从合适的接口转发出去
- 转发表 = 精简版路由表。更精简的数据结构有助于快速检索

### 拥塞控制

#### 拥塞

- 原因: 网络上出现过量分组，超负荷，引起网络性能下降
- 现象: 网络上的分组数增加，但吞吐量反而降低
- 类比: 节假日路上的车辆增多到一定程度时，收费站的吞吐量反而降低

#### 拥塞控制方法

- 开环控制(静态方法)
  - 在部署网络时，京就提前设计好预防拥塞的方法。
  - 一旦网络系统开始运行，就不再修改
- 闭环控制(动态方法)
  - 动态监视网络状态，及时发现哪里发生拥塞，并将拥塞信息传递给相关路由器(如：通过ICMP)
  - 相关路由器及时调整“路由表”

## IPv4

### 各种协议的服务关系

<img src="C:\Users\30703\AppData\Roaming\Typora\typora-user-images\image-20250723152021497.png" alt="image-20250723152021497" style="zoom:50%;" />

- IP协议(`lnternet Protocol`，网际协议)是互联网的核心！
- ARP协议: 用于查询同一网络中的`<主机IP地址, MAC地址>`之间的映射关系
- ICMP协议用于网络层实体之间相互通知口“异常事件"
- IGMP协议用于实现IP组播

### IP分组

- 也叫IP数据报
- <img src="C:\Users\30703\AppData\Roaming\Typora\typora-user-images\image-20250723152731198.png" alt="image-20250723152731198" style="zoom:50%;" />
- <img src="C:\Users\30703\AppData\Roaming\Typora\typora-user-images\image-20250723152845880.png" alt="image-20250723152845880" style="zoom: 50%;" />
- 版本
  - 4bit
  - 用于区分网络层使用的IP协议版本（v4、v6）

- 首部长度
  - 4bit: 表示0~15
  - 以×4B为单位, 则最大为15x4B = 60B, 即首部最大为60B(固定部分占20B, 可变部分最多占40B)
- 区分服务: 8bit
- 总长度
  - 16bit
  - 总长度: 首部 + 数据部分 
  - 以×1B为单位

- 分片
  - 最大传送单元(`MTU`): 一个链路层数据帧能承载的最大数据量
  - 如果一个IP数据报的总长度超出了下一段链路的MTU，就需要分片
  - IP数据报的“分片”可能在源主机、或任何一个路由器中发生
  - 只有目的主机才会对分片进行“重组
  - 各分片有可能乱序到达目的主机
  - 由于首部的“片偏移”字段是以 ×8B 为单位，因此，除了最后一个分片外，其他每个分片的数据部分必须是 8B 的整数倍
  - 注意：每个分片都是一个可以被单独转发的IP数据报，都包含首部

- 标识
  - 16bit
  - 由IP数据报的源主机生成, 通常是自增序列

- 标志
  - 3bit
  - 最低位MF(`More Fragment`)
    - MF=1,表示后面还有分片
    - MF=0,表示这是最后一个分片

  - 次低位DF（`Don't Fragment`)
    - DF=1,表示不允许被分片
    - DF=0,表示允许被分片

  - 最高位不用管

- 片偏移
  - 表示数据部分在王“被分片前”的位置
  - 以×8B为单位

- 生存时间(`TTL`)
  - 数据报在网络中可通过的路由器数的最大值。
  - TTL的初始值通常由源主机设置
  - 每经过一个路由器，路由器就将TTL减1，如果TTL减到0就直接丢弃分组，并向源主机发送ICMP报文
  - `ICMP报文`用于通知一个节点发生了某种“异常”
- 协议
  - 8bit
  - 如果为TCP协议服务，设为6
  - 如果为UDP协议服务，设为17

- 首部校验和
  - 16bit
  - 每个路由器只校验首部, 而不校验数据部分
  - 如果该字段设为全0，表示不用校验
  - 校验和的计算方法与UDP相同

- 源地址
  - 32bit
  - 发送方的IP地址

- 目的地址
  - 32bit
  - 接收方的IP地址


## IP分类方案

- 单播地址: 分配给主机

  - A类: 
    - (1~126)
      <img src="C:\Users\30703\AppData\Roaming\Typora\typora-user-images\image-20250724144414278.png" alt="image-20250724144414278" style="zoom:50%;" />
  - B类: 
    - (128~191)
      <img src="C:\Users\30703\AppData\Roaming\Typora\typora-user-images\image-20250724144448349.png" alt="image-20250724144448349" style="zoom:50%;" />
  - C类: 
    - (192~223)
      <img src="C:\Users\30703\AppData\Roaming\Typora\typora-user-images\image-20250724144513455.png" alt="image-20250724144513455" style="zoom:50%;" />

- 多播地址: 分组(有若干个主机)

  - D类:
    - (224~239)
      <img src="C:\Users\30703\AppData\Roaming\Typora\typora-user-images\image-20250724144613223.png" alt="image-20250724144613223" style="zoom:50%;" />
  - E类: 
    - (240~255)
      <img src="C:\Users\30703\AppData\Roaming\Typora\typora-user-images\image-20250724144640219.png" alt="image-20250724144640219" style="zoom:50%;" />

  

  ![image-20250724165537605](C:\Users\30703\AppData\Roaming\Typora\typora-user-images\image-20250724165537605.png)

- 以上这些特殊地址不能指派给网路中的任何一台主机或路由器“私用’

- 重要结论前两行说明，如果一个网络中，主机号占Nbit那么这个网络中，
  最多支持2N-2台台主机&路由器

## 子网划分技术

### 子网划分

1. 原理
   - 若某单位租用了一个IP地址段，假设原本主机号占nbit，那么可以将前kjit
     抠出来作为子网号，用剩余的 n-k bit作为主机号，这样就能划分出 2^k 个子网（每
     个子网包含的IP地址块大小相等）
2. 子网划分前，IP地址为两级结构=`<网络号,主机号>`
3. 子网划分后，IP地址为三级结构=`<网络号,子网号，主机号>`
4. 每个子网地址中，主机号不能分配为全0/全1一全0表示子网本身，全1为子网广播地址

### 子网掩码

1. 作用
   - 用子网掩码、IP地址"逐位与"，算出<网络号，子网号>（可合称为“网络前缀”)
   - 只有网络前缀相同的IP地址，才归属于同一个网络(或子网)
2. 如果一个网络内部进行了子网划分，那么这个网络中的每台主机、每个路由器接口都需要配置IP地址、默认网关、子网掩码
3. 如果一台路由器支持子网划分技术那么在它的转发表中，需要包含<目的网络号，子网掩码转发接口>

### 默认子网掩码

1. 如果一个传统网络(A/B/C类)内部没有进行子网划分，那么可以将对应此网络的转发表项设置为“默认子网掩码”
2. A类默认255.0.0.0
3. B类默认255.255.0.0
4. C类默认 255.255.255.0

### 默认路由

1. 默认路由(默认转发表项）设置：<目的网络号全0，子网掩码全0>
2. 在路由器转发表中，如果所有表项都不匹配，那么将从“默认路由"转发出去

## CIDR

### 时代背景

​	1993年推出无分类编址CIDR，当时互联网在民用领域大放异彩,由于每台主机都要消耗一个全球唯一的IP地址。IP地址资源告急。

#### 无分类编制CIDR

![image-20250725085145883](C:\Users\30703\AppData\Roaming\Typora\typora-user-images\image-20250725085145883.png)

---

### 基本原理

- 取消了IP地址传统的 A/B/C/D/E 分类。采用无分类编址CIDR，IP地址块`分配更灵活`，`利用率更高`，一定程度上缓解了IP地址耗尽
- IP地址 ={`<网络前缀>,<主机号>`}，其中网络前缀不定长
- CIDR记法一`128.14.32.153/30`，表示在这个IP地址中，`网络前缀`占30bit，`主机号`2bit

一个单位获得CIDR地址块后，可以把它划分为多个子网

### 定长子网划分

- 定义：在一个CIDR地址块中，划分子网时，子网号长度不固定（每个子网包含的IP地址块大小不同)

### 变长子网划分

- ​	定义：在一个CIDR地址块中，划分子网时，于子网号长度不固定(每个子网包含的IP地址块大小不同)

==注意：在每个子网中，主机号全0、全1的IP地址不能分配给特定节点私用==

### CIDR地址块的子网划分技巧

- `原始CIDR地址块`作为`根节点`
- 每个分支节点必须`同时拥有左右孩子`，左0，右1(反过来也可以)
- 每个`叶子节点对应一个子网`, 根据根节点到达叶子节点的路径来分析子网对应的IP地址块范围
- 整棵树的高度不能超过h-1(h: 可以自由分配的主机号占`h` bit)——因为即便最小的子网也至少要保留 2bit 主机号

## 路由聚合

- 定义: 对于一个`路由转发表`，如果几条路由表项的`转发接口相同`部分`网络前缀也相同`，那么可以将这几条路由表项`聚合为一条`。这种地址的聚合称为`路由聚合`，也称构成`超网`
- 优点
  - 路由聚合可以减少路由表的大小
  - 路由表更小，查询更快
- 缺点
  - 路由聚合可能会引入额外的无效地址
  - 最长前缀匹配原则
    - 由路由聚合引出的问题
    - 当路由表中有多个匹配的地址时, 选择匹配的前缀最长的对应接口转发

## NAT

- NAT():网络地址转换
- 端口号(传输层): 指明主机上的一个特定的进程

### 私有IP地址

- 每个局域网内部都可以自行分配这些私有IP地址
- 私有IP地址是可复用的，只要求局域网内唯一，不要求全球唯一

### 全球IP地址(外网IP)

- 通常由ISP提供,全球唯一
- 外网IP 是一个局域网与外界通信时所需使用的IP地址

### NAT路由器

- 作用：转发IP数据报时，进行内网IP、外网IP的相互转换
- NAT表，记录地址转换关系<内网IP：端口号外网IP：端口号>
- 一个IP数据报
  - 从内网转发到外网会更改源IP地址源端口号
  - 从外网转发到内网，会更改目的IP地址、目的端口号
- NAT路由器包含传输层的功能(因为端口号是传输层的概念）

### 地址解析协议ARP

- `ARP协议`用于查询同一网络中的`<IP地址,MAC地址>`之间的映射关系
- ARP表(ARP缓存)
  - 记录(IP地址+MAC地址）之间的映射关系
  - 一个数据结构（每台主机、每台路由器都有自己的ARP表）
  - 需要定期更新ARP表项
- 过程
  - ARP请求分组
    - ARP请求分组封装进MAC帧(帧目的地址=全1，源地址=Y)(广播帧)
    - 指明源IP地址, 源MAX地址
    - 指明目的IP地址
  - ARP响应分组
    - ARP响应分组封装进MAC帧（帧目的地址=Y，源地址=V)(单播帧)
    - 响应源IP地址, 源MAC地址

<img src="D:\refines\refines\PixPin_2025-07-29_16-53-05.png" alt="PixPin_2025-07-29_16-53-05" style="zoom: 50%;" />

## DHCP

- 动态主机配置协议( `Dynamic HostConfiguration Protocol`,DHCP常用于给主机动态分配IP 地址、配置子网掩码、默认网关等网络相关的参数

- DHCP是`应用层协议`，使用`UDP`的提供的服务

- DHCP使用客户/服务器模型(C/S)
  - DHCP客户: 就是新接入网络的主机（希望获得IP地址等配置）

  - DHCP服务器
    - 就是负责分配IP地址的那台主机，管理一系列IP地址池

    - 注：在家庭网络中，通常由家庭路由器兼职"DHCP服务器

    - 在一个大型网络内可以有多台DHCP服务器

- 报文

  1. 发现报文(`discover`)

     - 设备 -> DHCP服务器

     - 新设备向DHCP服务器发送设备的MAC地址, 并请求分配IP地址
     - 对应的MAC帧为广播帧
     - 网络层：`源IP地址=0.0.0.0`，`目的IP地址=255.255.255.255`（广播IP数据报）
     - 链路层：源MAC=客户的MAC地址，`目的MAC=FF.FF.FF.FF.FF.FF`(全1)(广播帧)

  2. 提供报文(`offer`)

     - DHCP服务器 -> 设备
     - DHCP服务器分配IP地址, 子网掩码, 默认网关, 租用期
     - 对应的MAX帧为单播帧
     - 网络层：源IP=DHCP服务器的IP地址，`目的IP=255.255.255.255`（广播IP数据报）
     - 链路层：源MAC=服务器的MAC地址，目的MAC=客户的MAC地址（单播帧）

  3. 请求报文(`request`)

     - 设备 -> DHCP服务器
     - 设备接收分配的IP地址
     - 对应的MAX帧为广播帧
       - 因为大型网络中, 可能会有多个DHCP服务器, 都会给新设备分配IP地址, 但新设备只会确认一个IP地址
       - 所以设备发送请求报文时, 要广播告诉所有DHCP服务器他确定的IP地址
     - 网络层：`源IP=0.0.0.0`，`目的IP=255.255.255.255`（广播IP数据报）
     - 链路层：源MAC=客户的MAC地址，目的MAC=全11 (广播帧)

  4. 确认报文(`acknowledge`)

     - DHCP服务器 -> 设备
     - DHCP服务器确认设备的IP地址, 子网掩码, 默认网关, 租用期
     - 网络层：源IP=DHCP服务器的IP地址，，`目的IP=255.255.255.255`（广播IP数据报)
     - 链路层：源MAC=服务器的MAC地址，目的MAC=客户的MAC地址（单播帧)


## ICMP

网际控制报文协议( `Internet Control Message Protocol`)

- ICMP属于网络层, ICMP报文封装在IP数据报中
- 作用: ICMP可以让主机或路由器互相报告网络中发生的差错和异常情况
- ICMP报文
  1. 差错报告报文
     - 终点不可达
       - 路由器告诉发送方：“目的IP地址不可到达”(道路不通）
       - 目的主机告诉发送方：“目的端口号不存在，我这边没有对应进程”
     - 时间超时
       - 路由器告诉发送方：“你的IP数据报到我这里TTL=0，被我丢了”(路程太远)
       - 目的主机告诉发送方：“你的IP数据报被分片了，规定时间内没到齐，我已全部丢弃”
     - 参数问题: 告诉发送方：“你的IP数据报首部参数不合法、或首部校验出错
     - 改变路由 (重定向): 路由器告诉发送方：“对于这个目的网络，下次你让另一台路由器帮你转发，路径会更短”
  2. 询问报文
     - 回送请求(`Echo Request`)
     - 回送回答(`Echo Reply`)
     - 时间戳请求(`Timestamp Request`)
     - 时间戳回答(`Timestamp Reply`)
  3. 不必反馈ICMP差错报文的情况
     - 若携带ICMP差错报告报文的IP数据报出错，不再反馈ICMP差错
     - 若IP数据报被`分片`，则无论几个分片出错，都只反馈`一次`ICMP差错
     - 若IP数据报的目的地址为`多播`地址，`不反馈`ICMP差错
     - 若IP数据报的源地址为`特殊地址`（如：127.X.X.X、0.0.0.0），则即便发生IP数据报异常也`不反馈`ICMP差错
  4. ICMP典型应用
     - `ping `基于 回送请求报文、回送回答报文 实现功能
     - `traceroute`(tracert)基于时间超过报文实现功能

## IPV6

### 背景

- 1981年诞生IPv4
- IPv4地址 = 32位
  $2^{32} \approx 42亿个地址$
- 1994年诞生的NAT只能缓解IP地址耗尽

### IPv6

- 1998年诞生
- IPv6地址=128位
- $2^{128} \approx 3.4 \times 10^{38}个地址$
- 平均每人可得$4.3 \times 10^{28}$个地址
- IPv6能彻底解决IP地址不够用的问题

### 冒号十六进制记法

- 总共128bit地址，16bit为一段，记录为十六进制，段间以冒号分隔

### 压缩记法

1. 去除每个分段的前导零
   - 每个16位段内的前导零可以省略
2. 用双冒号“`::`"替代连续出现的n多个0
   - 注意一个地址中只能出现一次双冒号，否则会产生歧义

- 一级ISP![PixPin_2025-07-30_10-08-55](D:\refines\refines\PixPin_2025-07-30_10-08-55.png)
- 二级ISP
  ![PixPin_2025-07-30_10-10-26](D:\refines\refines\PixPin_2025-07-30_10-10-26.png)

### 优点

- IPv6 因为地址空间很大, 所以可以划分为更多的层次
- Pv6支持即插即用（IP地址自动配置)-一一台主机接入网络后，只需向路由器确认本网络前缀，就可以给自己分配IP地址（通常以主机自身的MAC地址作为接口标识符）<img src="D:\refines\refines\PixPin_2025-07-30_10-13-31.png" alt="PixPin_2025-07-30_10-13-31" style="zoom:50%;" />
- IPv6 可以不使用 DHCP。当然，在一些IP地址安全性要求高的地方，七也支持使用 DHCP统一管理IPv6 地址
- 支持无分类编址CIDR，因此各级地址管理机构可以灵活划分子网大小

### 分类

![PixPin_2025-07-30_10-14-52](D:\refines\refines\PixPin_2025-07-30_10-14-52.png)

IPv6数据报的目的地址有以下三种基本类型：

1. 单播（`unicast`）。就是传统的点对点通信。应用：网络会议
2. 多播（`multicast`）。一点对多点的通信，数据报发送到一组计算机中的每一台。
3. 任播（`anycast`）。这是IPv6 增加的一种类型。任播的终点是一组计算机，但数据报只交付其中的一台计算机，通常是距离最近的一台计算机. 应用：多个DNS服务器共享一个任播地址
   注：任播地址没有固定的前缀，通常由地址管理机构预先统一分配。

## 路由算法

### 路由算法、路由协议之间的关系

<img src="D:\refines\refines\PixPin_2025-07-30_17-16-07.png" alt="PixPin_2025-07-30_17-16-07" style="zoom:50%;" />

- 术语别名：路由协议=路由选择协议;。路由算法=路由选择算法
- 路由算法的目标是找到转发IP分组的"最佳路径”。
- 路由协议实现了某种路由算法，并且还要定义路由器之间如何相互通信，以获取路由算法所需的信息

### 分类

1. 静态路由算法
   - 指由网络管理员手工配置每一条路由
   - 实现简单、开销小，不具备自适应能力。适用于小型网络
2. 动态路由算法。
   - 路由器根据网络流量负载和拓扑结构的变化来动态调整自身的路由表。
   - 考研大纲要求掌握的动态路由算法是：`距离-向量路由算法`和`链路状态路由算法`, `路径-向量路由算法`
     - 距离-向量路由算法
       - (RIP基于此算法)
       - 假设`d(net1)`表示从节点×到节点 net1的最短距离（最小代价），则有`dx(net1) = min{ c(x, v) + d (net1)}`(其中，v是×的所有邻居)
       - 特点：路由器不必关心完整的网络拓扑结构。只需关心和邻居之间的距离、各个邻居和目的网络之间的最短距离
       - 本质是Bellman-Ford算法在路由领域的应用
     - 链路状态路由算法
       - (OSPF基于此算法)
       - 本质是迪杰斯特拉Dijkstra算法在路由领域的应用
       - 只要一台路由器知道完整的`网络拓扑结构`（数据结构：带权图就可以用“迪杰斯特拉Dijkstra)算法”找到最优转发路径
       - 特点：路由器需要知道完整的网络拓扑结构，再使用迪杰斯特拉算法找到最短路径
     - 路径-向量路由算法
       - (BGP基于此算法)
   - 实现复杂、开销大、具备自适应能力。适用于大型网络

### RIP路由协议

- 基于距离向量路由算法。路由器要把自身的“距离向量”告诉邻居们。网络数量越多，距离向量越大

### OSPF路由协议

- 基于链路状态路由算法，要求每一台路由器都要建立整个网络的拓扑图。
- 全世界的路由器数量、网络数量数以亿计)不可能让每台路由器都知晓全世界的网络情况。

### 分层次的路由协议

- 将全世界的网络划分为多个相互独立的自治系统。例如，由咸鱼电信管辖的网络区域可申请成为自治系统AS(`Autonomous System `)
- AS的管理单位有权决定在本自治系统内，使用何种内部路由协议（如：RIPOSPF)
- 各边界路由器之间，使用统一的外部路由协议（如：BGP-4)
- 边界路由器
  - 性能强悍，价格高昂
  - 边界路由器之间使用BGP协议
- 自治系统之间的路由选择也称域间路由选择，自治系统内部的路由选择也称域内路由选择
- 全世界目前已接近8w个自治系统
- 每个自治系统拥有全球唯一的AS编号CAutonomous System Number,简称ASN需向互联网管理机构申请
- 自治系统之间是平级关系，不存在包含关系
- 一个自治系统通常包含一个或多个CIDR地址块央 （便于路由聚合）

#### 分类

1. 内部网关协议`(Interior GatewayProtocol,IGP）`用于AS内部的路由选择。例如：RIP、OSPF

2. 外部网关协议`(External GatewayProtocol`,EGP用于AS之间的路由选择。例如：:BGP

> - 网关=路由器（早期术语沿用至今）
> - 内部网关协议（各地方言)
> - 外部网关协议义=普通话

# 传输层

## 端口

- 通过“端口号”标识本主机的一个特定进程
  - TCP、UDP的端口号是`相互独立的`
  - 每台主机的端口号是`相互独立的`
- 当两个进程之间想要通信时，需要指明：
  1. 使用哪种传输层协议；
  2. 本进程绑定的端口号；
  3. 对方的IP地址和端口号：
- TCP或UDP协议，通过Socket 套接字=`{IP地址：端口号}`，唯一地标识网络中的一台主机上的一个应用进程
- 端口号分类
  1. 服务器使用的端口号
     - 熟知端口号: 0~1023   通常只能用于被熟知的重要应用程序
     - 登记端口号: 1024~49151
  2. 客户端使用的端口号
     - 短暂端口号: 49152~65535

## 功能

- 实现端到端（进程到进程）的通信
- 复用和分用
  - 复用（从上到下）：在发送数据的时候，同一台主机上的多个进程可以使用同一个传输层协议
  - 分用（从下到上）：在接收数据的时候，传输层可以把数据正确交付到目的进程
- 差错检测
  - TCP检测出差错后丢弃数据并`通知发送方重传`
  - UDP检测出错误后直接丢弃数据且`不通知发送方`
- 向应用层提供两种服务
  - 面向连接的、可靠的端到端传输服务（TCP）一确保数据正确/完整，但开销大、实时性较差
  - 无连接的、不可靠的端到端传输服务(UDP）一数据可能出错/丢失，但速度快、开销小

## UDP

### 报文

![PixPin_2025-07-31_09-27-36](D:\refines\refines\PixPin_2025-07-31_09-27-36.png)

- 首部: 8B
- UDP每次传输一个完整的报文，不支持报文自动拆分、重装
- UDP是无连接的、不可靠的（可靠性可以交给应用层处理），也不支持拥塞控制
- UDP支持一对一（封装成单播IP数据报（封装成广播/多播IP数据报）
- 报文:   应用层
  ![PixPin_2025-07-31_09-21-23](D:\refines\refines\PixPin_2025-07-31_09-21-23.png)
- UDP数据报:     传输层
  ![PixPin_2025-07-31_09-22-07](D:\refines\refines\PixPin_2025-07-31_09-22-07.png)
- IP数据报:   网络层
  ![PixPin_2025-07-31_09-23-03](D:\refines\refines\PixPin_2025-07-31_09-23-03.png)

### UDP校验

- 发送方

  1. 计算检验和之前，先`添加伪首部`; 计算完之后，去掉伪首部

  1. 把`伪首部、首部、数据`部分以`16bit为一组`，进行二进制加法（最高位产生的进位需要`回卷`)
     1. 回卷: 如果最高位产生进位，就“回卷"(加到最低位)

  1. 将最终的加法结果`逐位取反`，就得到16bit检验和，将其填入UDP首部

  1. 去掉伪首部，并将UDP数据报交给网络层，封装成IP数据报

- 接收方

  1. 网络层向传输层递交UDP数据报
  2. 传输层在UDP数据报之前，`添加伪首部`
  3. 把`伪首部、UDP首部、数据`部分以`16bit为一组`，进行二进制加法 （最高位产生的进位需要回卷)
  4. 如果加法结果为`全1`，说明`没有比特错误`，于是接收该UDP数据报，并根据目的端口号，向应用层递交报文。
     如果加法结果`不是全1`，说明`有差错`，于是`丢弃`该UDP数据报

## TCP

### TCP报文段

> TCP段, `TCP segment`

- 首部： 20~60B
- TCP支持报文自动拆分、重装，因此可以传输长报文
- TCP是有连接的、可靠的、支持拥塞控制
- TCP仅支持一对一传输 (因为通信双方的传输层必须先建立连接)

![PixPin_2025-07-31_16-02-27](D:\refines\refines\PixPin_2025-07-31_16-02-27.png)

1. 序号: 
   - 用于标记数据部分`第一个字节`在原始字节流中的位置
   - 起始“序号”是发送方`自己设置`的，不一定从0开始
   - "序号”在真题中常简记为小写的`seq`
2. 确认号:
   - 用于反馈，表示序号在该确认号之前的所有字节都已正确收到
   - `ack`, `ack_seq`
3. ==`ACK`==
   - 1bit（记为大写ACK）
   - ACK=0时，`ack_seq`无效
   - ACK=1时，`ack_seq`有效
4. 数据偏移
   - `data offset`
   - 4bit，表示TCP首部长度，以×4B为单位
5. 保留
   - 6bit,暂时没啥用，通常全部置为0
6. `URG`
   - 1bit，
   - URG=1时，`紧急指针`有效。表示这是`紧急数据`，应尽快插队发送
7. `PSH`
   - 1bit，
   - PSH=1时，表示希望接收方`尽快回复`（用于`交互式通信`)
8. `RST`
   - 1bit，
   - RST=1时，表示出现严重差错（如主机崩溃），必须`释放连接`。
   - 也可用于拒绝一个非法报文段（如恶意的黑客攻击）
9. ==`SYN`==
   - `synchronize`
   - 1bit
   - 当 SYN=1时表示这是一个连接请求或连接接受报文。
   - 只有握手①、握手②的`SYN=1`，其他所有TCP报文段都是`SYN=0`
10. ==`FIN`==
    - 1bit，
    - `finish`
    - 当FIN=1时，表明此报文段的发送方的数据已发送完毕，要求释放传输连接。
    - 只有挥手①、挥手③的`FIN=1`，其他所有TCP报文段都是`FIN=0`
11. 紧急指针
    - 紧急数据专用序号原理与上面那个“序号”字段相同
12. 窗口(`rwnd`, `rcvwmd`)
    - `receive window`
    - 16bit
    - 表示接收窗口的大小。即从本报文段首部中的ack_seq算起，挂接收方还能接收多少数据(以字节为单位)
13. 校验和
    - 原理与UDP雷同
    - 计算检验和之前也需要添加12B伪首部（只需将UDP伪首部的协议字段的17改成6，UDP长度字段改成TCP长度）
14. 选项
    - 建立TCP连接时，在握手①、握手②选项中协MSS（`Maximum Segment Size`，最大段长)
    - MSS的值表示在接下来的数据传输中，一个TCP报文段最多携带多少数据（首部不算在内）
    - 通常MSS不会设置太大，以免在IP层被分片

> 若SYN=1,可称为`SYN段`
> 若FIN=1, 可称为`FIN段`
> 若ACK=1,可称为`ACK段`

![PixPin_2025-07-31_16-03-17](D:\refines\refines\PixPin_2025-07-31_16-03-17.png)

### 三大阶段

> - 建立TCP连接时，双方协商jMSS=1000B（`Maximum Segment Size`，最大段长)
> - 每次建立TCP连接，可以传输多个报文 (双向)
> - TCP是面向字节流的（而UDP是面向报文的）无论传输多少个报文，在TCP协议看来都是一连串字节流
> - TCP并不会强制要求每个TCP段都满载数据，只要别超过MSS就行

![TCP流程](D:\SXU\考研\计算机网络\TCP流程.jpg)

1. 建立连接(三次握手)
   - 三次握手, 对应3个TCP报文段
2. 数据传输
   - 全双工通信
3. 释放连接(四次挥手)
   - A没有数据要发送, 两次挥手断开连接
   - 此时只有B能继续给A发送TCP报文段
   - B没有数据要发送, 两次挥手断开连接

### 连接管理

- 在TCP全过程中，只有握手①、握手②的`SYN=1`
- 在TCP全过程中，只有`握手①的ACK=0`, 握手①、握手②`不能携带数据`（只有TCP首部），但是仍要`消耗一个序号`)
- 握手③如果`不携带数据`就`不消耗序号`
- 如果握手③携带100B数据那么消耗100个序号
- 在TCP全过程中，只有挥手①、挥手③的`FIN=1`
- 挥手①、挥手③即使`不携带数据`, 也要`消耗一个序号`(因为是`FIN段`)
- 挥手①、挥手②, 挥手③`可以`携带数据
- 挥手④`不可以`携带数据
- 也可以是服务器先发出挥手
- 客户进程收到挥手③后，立即进入`TIME-WAIT`状态，并启动“`TIME-WAIT计时器`”倒计时`2MSL`后才能进入`CLOSE状态`（如果等待期间`重复收到挥手3`),就`重置计时器`
- MSL( `Maximum Segment Lifetime`，最长报文段寿命），是由TCP协议规定的十个固定时间长度
- 如果服务器进程收到挥手①时，己经没有待传送数据，那么可以`连续发出`挥手②、挥手③, 此时的`CLOSE-WAIT`极短暂, 视为0

#### 考点

1. 从发出握手①，到客户端进程可以发送数据，至少需要多久?
   - 1RTT
2. 从发出握手①，至到服务器进程可以发送数据，至少需要多久？
   - 1.5RTT
3. 从客户发出挥手①，到客户进程进入CLOSE状态，至少需要多久？
   - 1RTT+2MSL
4. 从客户发出握手①，到服务器进程进程进入CLOSE状态，至少需要多久？
   - 1.5RTT

### 可靠传输和流量控制

> `流量控制`：控制端到端的数据发送量，是`“局部的"`

- 发送窗口的大小不能超过发送缓冲区的大小
- 发送窗口的大小也不能超过接收窗口的大小
- 接收窗口不能大于接收缓冲区大小
- 一个端口可以建立多个TCP连接, 但每个TCP连接仅支持一对一的全双工通信

1. 握手1, 设置客户进程初始序号
2. 握手2, 设置服务器初始序号, 反馈接收窗口大小
3. 握手3, 客户进程反馈接收窗口
4. 服务器收到数据
   - 累积确认：若服务器连续收到两个报文段, 只返回一个ACK
   - 服务器收到一个新报文段，接收缓冲区满，此时应尽快将数据按序交付应用层，清空缓冲区
   - 捎带确认：服务器刚好有5B数据要发送，就立即返回ACK段，并捎带数据
   - 虽然缓冲区未满，也可以按序交付应用层
   - 超时重传机制: 每发出一个报文段，就设置超时重传计时器, 若超时还没有收到回复(`ACK段`), 就重新发送并重置计时器
   - 快重传(`冗余ACK`): 
     - 作用: 让可能出错的报文段尽早重传而不是非要等到超时再重传
     - 如果发送方收到三个确认号相同的(冗余, 不包括第一个正常的)ACK，就立即重发对应的报文段
     - 立即确认: 每收到一个报文段就立即确认，即使是失序报文段，也要`立即返回ACK`, (失序报文段会导致冗余ACK)
5. 服务器返回最新确认号, 最新接收窗口大小

### 拥塞控制

==重点看图理解, 期末大题必考!!!==

> `拥塞控制`：控制整个网络中每台主机的数据发送量，降低路由器负载，是`“全局的"`

如何判断拥塞?

- 严重拥塞: 发出的报文段未能按时收到ACK，引发超时重传
- 有点拥塞: 收到冗余ACK,引发快重传

如何解决?

- 严重拥塞, 就`迅速缩小`拥塞窗口
- 有点拥塞, 就`适当缩小`拥塞窗口

#### 慢开始算法

- `cwnd`(拥塞窗口) 值从1开始，每收到一个ACK，就让`cwnd+1`（当cwnd<ssthress时适用）

#### 拥塞避免算法

- 在一个RTT内，即使收到多个ACK，也只能让`cwnd+1`（当cwnd≥ssthress时适用）

#### 快恢复算法

- 一旦发生快重传，就将`阈值、cwnd `都设为当前`cwnd的一半`，然后切换到为`“拥塞避免算法”`

---

# 应用层

## 网络应用模型

- 客户/服务器(C/S)模型
  - 服务器: 提供计算服务的设备
    1. 永久提供服务
    2. 永久性访问地址/域名
  - 客户机: 请求计算服务的主机。
    1. 与服务器通信，使用服务器提供的服务
    2. `间歇性接入网络`
    3. 可能使用动态IP地址
    4. 不与其他客户机直接通信
  - 特点
    - 客户、服务器地位不平等
    - 客户之间`不直接通信`
    - 如果服务器损坏，影响全局
    - 服务器`负载较大`
- P2P模型
  - 不存在永远在线的服务器
  - 每个主机既可以提供服务，也可以请求服务
  - 任意端系统/节点之间可以直接通讯
  - 节点间歇性接入网络
  - 节点可能改变IP地址
  - 可扩展性好
  - 网络`健壮性强`
  - 特点
    - 主机之间地位`平等`
    - 主机之间`直接通信`
    - 单个节点损坏不影响全局
    - 各节点可以`分摊负载`

## DNS

### 域名

1. 根
2. 顶级域名
   - 国家顶级域名`cn,us,uk`
   - 通用顶级域名`com,net,org,gov,int,aero,museum,travel`
   - 基础结构域名/反向域名arpa
3. 二级域名
   - 类别域名`ac,com,edu,gov,mil,net,org`
   - 行政区域名 用于我国各省、自治区、直辖市 bjs
   - 自定义注册
4. 三级域名
5. 四级域名

### 域名服务器

- 根域名服务器
- 顶级域名服务器
  - 管理该顶级域名服务器注册的所有二级域名
- 权限域名服务器
  - 负责一个区的域名服务器
- 本地域名服务器(默认域名服务器)
  - 当一个主机发出DNS查询请求时，这个查询请求报文就发给本地域名服务器。
  - `缓存`本地请求过的域名

### 域名解析过程

- 递归查询
- 迭代查询

## FTP

- FTP(`File Transfer Protocol`)
- FTP是基于客户/服务器（C/S）的协议。
- 用户通过一个客户机程序连接至在远程计算机上运行的服务器程序。
- 依照FTP 协议提供服务，进行文件传送的计算机就是FTP服务器。
- 连接FTP服务器，遵循FTP协议与服务器传送文件的电脑就是FTP客户端。
- FTP使用TCP实现可靠传输

### 工作原理

1. 登陆FTP地址
   1. 用户名&密码
   2. 匿名登录
      1. 互连网中有很大一部分FTP服务器被称为`“匿名”（Anonymous）`FTP服务器。这类服务器的目的是`向公众提供文件拷贝服务`，不要求用户事先在该服务器进行登记注册，也不用取得FTP服务器的授权。
      2. `Anonymous（匿名文件传输）`能够使用户与远程主机建立连接并以匿名身份从远程主机上`拷贝文件`，而不必是该远程主机的注册用户。用户使用特殊的用户名“anonymous"登陆FTP服务，就可访问远程主机上公开的文件。
2. FTP服务器进程
   - 1个主进程
   - n个从属进程
3. FTP传输模式
   - 文本模式：ASCII模式，以文本序列传输数据：
   - 二进制模式：Binary模式，以二进制序列传输数据，

## WWW

- 万维网WWW（`World Wide Web`）是一个大规模的、联机式的信息储藏所/资料空间，是无数个网络站点和网页的集合。
- 万维网以`客户/服务器`方式工作，用户使用的浏览器就是万维网客户程序，万维网文档所驻留的主机运行服务器程序。
- 组成
  - 统一资源定位符`URL `-> 唯一标识 -> 资源（文字、视频、音频...)
    - `URL`指向万维网上的某个特定资源
    - 端口、路径可以省去，因为有默认值
    - `URL`不区分大小写。
    - URL一般形式：
      - `<协议>://<主机>:<端口>/<路径>`
      - 协议: http, ftp.......
      - 主机: 域名, IP地址
  - 超文本传输协议`HTTP`
    - 应用层，基于`TCP`
  - 超文本标记语言`HTML`
    - 一种文档结构的标记语言，使用一些约定的标记对页面上的各种信息（包括文字、图像、视频等）、格式进行描述

## HTTP

- 超文本传输协议
- HTTP协议定义了浏览器（万维网客户进程）怎样向万维网服务器请求万维网文档，以及服务器怎样把文档传送给浏览器。
- 特点
  - HTTP协议是`无状态的`
  - HTTP采用`TCP`作为运输层协议，但HTTP协议本身是无连接的（通信双方在交换HTTP报文之前不需要先建立HTTP连
    接）。
- 连接方式
  - 持久连接(`keep-live`)
    - 特点：每建立一次TCP连接，可以完成多组HTTP请求&响应
    - 分类
      - 非流水线
        - 只有上一次HTTP请求收到响应后，才能发出下一次HTTP请求
      - 流水线
        - 可以连续发出多个HTTP请求
  - 非持久连接(`Close`)
    - 特点：每建立一次TCP连接，仅`完成一组`HTTP请求&响应
    - 可以通过“`并行TCP`连接"加快获取n个网页元素的速度
  - `HTTP/1.0`默认采用`非持续连接`
  - `HTTP/1.1`默认采用`持续连接`
  - (除非在HTTP请求报文中设置参数` Connection: Close`）
  - 默认端口`80`
- http报文
  - 请求报文	
    <img src="D:\refines\refines\PixPin_2025-08-01_10-36-10.png" alt="PixPin_2025-08-01_10-36-10" style="zoom:50%;" />
    - 方法: 希望服务器做什么？如：GET、POST
      <img src="D:\refines\refines\PixPin_2025-08-01_11-13-56.png" alt="PixPin_2025-08-01_11-13-56" style="zoom:50%;" />
      - `URL`: 指向的资源
      - 版本: 指明HTTP协议的版本. 如：HHTTP/1.0HTTP/1.1
  - 响应报文
    <img src="D:\refines\refines\PixPin_2025-08-01_10-36-36.png" alt="PixPin_2025-08-01_10-36-36" style="zoom:50%;" />
    - 状态码
      <img src="D:\refines\refines\PixPin_2025-08-01_11-15-14.png" alt="PixPin_2025-08-01_11-15-14" style="zoom:50%;" />
    - 短语: 如：404 Not Found
    - 实体主体: 存放需要返回的文件
- 浏览器访问一个网站的过程
  1. 用户输入网址 (域名)
  2. 浏览器通过`DNS服务器`查询域名对应的`IP地址`（浏览器会将查询结果“域名→IP"`缓存在本地`）
  3. 浏览器与`<Web服务器IP地址：80端口>`建立`TCP`连接
  4. 浏览器在==握手③==中携带`HTTP请求报文`（指明要访问哪个htmI网页）
  5. 服务器返回`HTTP响应报文`（`携带htmI`文件）
  6. 如果html引用了其他n个元素，还需要`n组HTTP请求&响应`(持续、非持续工作方式有所区别)

## SMTP, POP3

### 电子邮件

1. 用户代理
   - 电子邮件客户端软件
   - 使用`SMTP协议`
   - 通过`TCP`连接
   - 功能
     1. 撰写
     2. 显示
     3. 处理
     4. 通信
2. 邮件服务器
   - 作为发送方
     1. 作用: 发送邮件, 向发件人报告邮件传送结果
     2. 使用`SMTP协议`, 通过`TCP`连接
   - 作为接收方
     1. 接收邮件
     2. 使用`POP3`, `IMAP`协议

### SMTP

- 规定了在两个相互通信的SMTP进程之间应如何交换信息。
- 负责发送邮件的SMTP进程就是SMTP客户，
- 负责接收邮件的进程就是SMTP服务器。
- SMTP规定了14条命令（几个字母）和21种应答信息（三位数字代码+简单文字说明）
- 通过`TCP`连接
- 端口号为`25`
- 使用`C/S`网络应用模型
- 三个阶段
  1. 连接建立
  2. 邮件传送
  3. 连接释放
- 缺点
  - SMTP不能传送可执行文件或者其他二进制对象
  - SMTP仅限于`传送7位ASCII码`，不能传送其他非英语国家的文字。
  - SMTP服务器会拒绝超过一定长度的邮件。
  - 解决
    - 通用`因特网邮件扩充MIME`: 将各种文件转化为ASCII码, 使得传输内容更多样

### POP3

- 接收到邮件后, 下载并保留在服务器
- 用户接收到邮件后下载的客户机并服务器上删除

### IMAP

- 当用户打开IMAP服务器的邮箱时，用户可以看到`邮箱的首部`, 若用户需要`打开`某个邮件，该邮件`才上传`到用户的计算机上。
- IMAP可以让用户在`不同的地方`使用`不同的计算机`随时上网阅读处理邮件，还允许只读取邮件中的某一个部分（先看正文，有WiFi的时候再下载附件）。

````
